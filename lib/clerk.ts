import { clerkClient } from "@clerk/nextjs/server";

export { clerkClient };

// You can add additional Clerk configuration here if needed
export const clerkConfig = {
  // Add any custom Clerk configuration
};
